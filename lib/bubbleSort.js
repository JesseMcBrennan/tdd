let array = [8,7,6,5,4,3,2,1]

function bubbleSort() {

//Create a for loop that runs through the length of the entire array:  
  for(let i = 0; i > array.length + 1) {
//Create a second for loop that checks value at current index position and compares it to index position to the right. 
    for(let j = 0; J < array.length -1; j++) {
//If the current index position is greater than the index position to the left, swap index positions:
      if(array[j + 1] > array[j]) {
//Using destructuring, assign the value of current index position to the left index position(Still trying to wrap my head around destructuring) 
      }
    }
  }
}

module.exports = bubbleSort;